<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.s528748.trackerapp">

<!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->

<permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

<!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

<application android:name="android.support.multidex.MultiDexApplication" android:allowBackup="true"
    android:icon="@drawable/icon" android:label="@string/app_name" android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true" android:theme="@style/AppTheme">

    <activity android:name=".Home" />
    // Added SignUpOne Activity
    <activity android:name=".SignUpOne" />
    <activity android:name=".SettingsActivity" />
    <activity android:name=".LogOutActivity" />
    <activity android:name=".AboutUsActivity" />
    <activity android:name=".AddFriend" />
    <activity android:name=".TrackFriend" />
    <activity android:name=".TrackFriendOnMap" />
    <activity android:name=".PublicUserLocation" />
    <activity android:name=".ListOnline" />

    <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
    <meta-data android:name="com.google.android.geo.API_KEY" android:value="@string/google_maps_key" />

    <activity android:name=".MapsActivity" android:label="@string/title_activity_maps" />
    <activity android:name=".MapsActivity_TrackFriend" android:label="@string/title_activity_maps__track_friend" />
    <activity android:name=".ListOfFriends" />
    <activity android:name=".TrackFriendZero" android:label="@string/title_activity_track_friend_zero" />
    <activity android:name=".TrackFriendOne" android:label="@string/title_activity_track_friend_one" />
    <activity android:name=".TrackFriendTwo" android:label="@string/title_activity_track_friend_two" />
    <activity android:name=".TrackFriendThree" android:label="@string/title_activity_track_friend_three" />
    <activity android:name=".SignupNew" />
    <activity android:name=".MainActivityOne" android:label="@string/title_activity_main_one"
        android:theme="@style/AppTheme">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
</application>

</manifest>
